## Usage (Immutable)

- This file is used to provide instruction to the agent
- The code gen agent must update this file to capture DX preferences and codebase description
- Put your long term memory stuff about the project and how we like to operate below here as you see fit!

## Amendments (Mutable)

This is a single-file `index.html` static landing page for the Yes–No–Yes book.

DX preferences:
- Keep it tool-free: no build step, no bundlers, no npm.
- Use Tailwind via CDN and utility classes; avoid custom CSS files.
- Prefer editing `index.html` directly and keep copy/layout in that file.
- Keep the layout single-column with wide margins for reading.
